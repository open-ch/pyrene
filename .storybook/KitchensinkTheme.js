import { create } from '@storybook/theming';
import logo from './pyrene-logo.svg'

export default create({
  base: 'light',
  brandTitle: 'Pyrene Storybook',
  brandUrl: 'https://open-ch.github.io/pyrene/',
  brandImage: logo,
});
