import { Meta } from '@storybook/addon-docs';
import Paragraph from '../kitchensink/src/app/common/PageElements/Paragraph/Paragraph';
import DescriptionBox from '../kitchensink/src/app/common/PageElements/DescriptionBox/DescriptionBox';
import { Texts } from './texts';

<Meta
  title="Foundations/Typography"
  hidden={false}
  parameters={{
    viewMode: 'docs',
    previewTabs: {
      canvas: { hidden: true }
    },
  }}
/>

<style>{`
  .title {
    margin-left: 0;
    margin-bottom: 24px;
    font-size: 32px;
    font-weight: 600;
    line-height: 1.5;
  }
  .footnote {
    font-size: 13px;
    line-height: 16px;
    padding-top: 24px;
  }

  .firaCondensed {
    font-family: FiraSans, sans-serif;
  }

  .fontStack {
    font-size: 32px;
    line-height: 64px;
    letter-spacing: -0.2px;
    color: #1D273B;
    background-color: #F0F2F5;
    border-radius: 4px;
    padding: 32px;
  }

  .footer  {
    padding-top: 24px;
  }

  .link {
    font-size: 16px;
    color: #0055FF;
  }
`}</style>

<div>
  <div>
    <div className="title">{Texts.pages.typography.title}</div>
    <DescriptionBox>
      {Texts.pages.typography.description.map(row => 
        <span key={row.text}>
          { row.format !== 'strong' ? <>{row.text}</> : <strong>{row.text}</strong> }
        </span>
        )
      }
    </DescriptionBox>
  </div>
  <div>
    { Texts.pages.typography.sections.map(section => 
      <Paragraph title={section.title} key={section.title}>
        <DescriptionBox>
          {section.description}
        </DescriptionBox>
        
        { section.subDescription &&
        <div>
          <div className='fontStack'>
            {section.subDescription.header}<br />
            <span className='firaCondensed'>{section.subDescription.text}</span>
          </div>
          <div className='footer'>
            { section.subDescription.footer.map(row =>
              <>
                {row.link && <a className='link' href={row.link} download='fonts.zip'>{row.text}</a>}
                {!row.link && <span>{row.text}</span>}
              </>)}
          </div>
          </div>
        }
      </Paragraph>
    )}
  </div>
</div>
