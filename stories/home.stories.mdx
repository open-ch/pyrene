import { Meta } from '@storybook/addon-docs';
import Paragraph from '../kitchensink/src/app/common/PageElements/Paragraph/Paragraph';
import DescriptionBox from '../kitchensink/src/app/common/PageElements/DescriptionBox/DescriptionBox';
import DesignSystemBox from '../kitchensink/src/app/common/PageElements/IntroductionElements/DesignSystemBox';
import Principle from '../kitchensink/src/app/common/PageElements/IntroductionElements/Principle';
import { Texts } from './texts';

<Meta
  title="Introduction"
  hidden={true}
  parameters={{
    viewMode: 'docs',
    previewTabs: {
      canvas: { hidden: true }
    },
  }}
/>

<style>{`
  .title {
    margin-left: 0;
    margin-bottom: 24px;
    font-size: 32px;
    font-weight: 600;
    line-height: 1.5;
  }

  .principlesGrid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    grid-row-gap: 40px;
  }

  .designSystemGrid {
    display: grid;
    grid-gap: 24px;
  }
`}</style>

<div>
  <div>
    <div className="title">{Texts.pages.introduction.title}</div>
    <DescriptionBox>
      {Texts.pages.introduction.description.map(row => <span key={row}>{row}<br/><br/></span>)}
    </DescriptionBox>
  </div>
  <div>
    <Paragraph title={Texts.pages.introduction.sections[0].title}>
      <DescriptionBox>
        {Texts.pages.introduction.sections[0].description}
      </DescriptionBox>
      <div className="designSystemGrid">
        {Texts.pages.introduction.sections[0].subSections.map((subSection, index) => 
          <DesignSystemBox key={subSection.title}
            number={Texts.pages.introduction.sections[0].subSections.length-index}
            icon={subSection.icon}
            title={subSection.title}
            description={subSection.description}
          />
        )}
      </div>
    </Paragraph>
    <Paragraph title={Texts.pages.introduction.sections[1].title}>
      <DescriptionBox>
        {Texts.pages.introduction.sections[1].description}
      </DescriptionBox>
      <div className="principlesGrid">
        {Texts.pages.introduction.sections[1].subSections.map(subSection => 
          <Principle key={subSection.title}
            icon={subSection.icon}
            title={subSection.title}
            description={subSection.description}
          />
        )}
      </div>
    </Paragraph>
  </div>
</div>
