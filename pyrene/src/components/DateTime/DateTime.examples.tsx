import React from 'react';
import { Example } from '../../examples/Example';
import { DateTimeProps } from './DateTime';

const DateTime: Example<DateTimeProps> = {};

DateTime.props = {};

DateTime.examples = [
  {
    props: { },
  },
];

DateTime.category = 'Other';

export default DateTime;
